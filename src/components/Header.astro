---
import Hamburger from "@/components/Hamburger.astro"

interface Props{
  url:string,
  name:string,
}
const {url,name} = Astro.props;
---
<div class=" z-50 w-full h-24 border-b flex justify-center items-center">

  <Hamburger/>
  <img 
    class="h-5/6"
    src={url} 
    alt={name}>
  
</div>